# SQLite to Postgres Migration

A tool to migrate data from SQLite databases to PostgreSQL.

## Requirements

- Python 3.14+
- `uv` package manager

## Installation

```bash
uv sync
```

## Usage

```bash
uv run python main.py
```

## Configuration

Update database connection settings in `.env`:


## Features

- Automatic schema migration
- Data transfer with validation
- Progress tracking